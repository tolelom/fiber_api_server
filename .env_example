PORT=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
DB_NAME=
JWT_SECRET=
ENVIRONMENT=development
REDIS_ADDR=localhost:6379
UPLOAD_DIR=./uploads/images

# Sentry DSN — 설정 시 5xx 에러를 외부로 리포팅. 비우면 slog 로컬 로그만.
SENTRY_DSN=
